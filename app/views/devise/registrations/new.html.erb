<div class="container">
  <div class=row>

    <h1>Join our community!</h1>
    <h3>Register</h3>

    <div class='col-sm-offset-4 col-sm-4'>

      <p>
        Once you've registered and logged in successfully, take
        some time to cruise the site and see how awesome our community
        is! When you're ready, click the "intros" link up top and submit
        your first post. Be an open book, or keep it brief and simple.
        This is your space to open up to our family. Let us know why you're
        here and what you need.
      </p>



      <!-- <%= bootstrap_form_for(new_user_session_path) do |f| %>
    <%= f.email_field :email %>
    <%= f.password_field :password %>
    <%= f.check_box :remember_me %>
    <%= f.submit "Log In" %>
  <% end %> -->
  <%= bootstrap_form_for(resource,
                                  as: resource_name,
                                  url: registration_path(resource_name)) do %>

              <%= f.text_field :name,
                                placeholder: 'username (will be shown publicly)',
                                class: 'form-control' %>

              <%= f.text_field :email,
                                placeholder: 'email',
                                class: 'form-control' %>

              <%= f.text_field :password,
                                placeholder: 'password',
                                class: 'form-control' %>


              <%= f.text_field :password_confirmation,
                                placeholder: 'password confirmation',
                                class: 'form-control' %>

              <%= f.submit "Sign up", class: 'btn sign-up-button' %>

          <% end %>


    </div>
  </div>
</div>
